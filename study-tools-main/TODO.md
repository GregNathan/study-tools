# TODO: Fix Login and Signup Issues

- [x] Update js/auth.js: In login() function, remove duplicate/incorrect lines and add localStorage.setItem("loggedInUser", user) after sessionStorage set.
- [x] Update js/auth.js: In logout() function, also remove localStorage "loggedInUser".
- [x] Update index.html: Remove duplicate <script src="js/auth.js"></script> tags, keeping only one in the head.
